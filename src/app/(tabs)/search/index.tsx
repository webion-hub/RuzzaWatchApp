// Route wrapper for the search tab. The native search bar lives in the screen
// (headerSearchBarOptions) + the Stack header in this folder's _layout.tsx.
export { default } from '@/components/screens/search';
