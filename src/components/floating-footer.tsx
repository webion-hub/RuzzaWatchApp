/**
 * The tab bar is now Expo Router's native tab bar (`src/app/(tabs)/_layout.tsx`),
 * which on iOS 26 floats with Liquid Glass. Screens still reserve this much space
 * at the bottom of their scroll content so the last items clear the floating bar.
 */
export const FLOATING_FOOTER_HEIGHT = 78;
